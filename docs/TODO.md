# Features

- GW0 season-long predictions — Golden Boot (confirmed: FPL autocomplete, 10 pts) + Premier League winner (potential; double points if unique correct pick). See [plan](plans/gw0-predictions.md).
- Multipick chip — pick 2 teams; flat win point if either wins (unique bonus if the winning team is unique); both teams get used up. See [plan](plans/multipick-chip.md).

# UX fixes

See [plan](plans/ux-fixes.md) — layout→tabs (all devices), Rules tab, fixtures on the Pick tab, and the league-table scored-only fix.

- chips played should only be included in league table after game week is scored
- table includes users points before game week done, but shouldnt.
- - i think because we limit so each person can only see their own stats before game week.  but this should not be the only filter on the league table.
- fixtures should be at the top in mobile
- we should have a rules page
- generally can we make things into grouped tabs where it makes sense rather than one big scroll?